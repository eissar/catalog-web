{
  "imports": {
    "lume/": "https://cdn.jsdelivr.net/gh/lumeland/lume@3.2.2/",
    "lume/jsx-runtime": "https://cdn.jsdelivr.net/gh/oscarotero/ssx@0.1.14/jsx-runtime.ts",
    "lume/cms/": "https://cdn.jsdelivr.net/gh/lumeland/cms@0.14.16/",
    "markdown-plugins/": "https://cdn.jsdelivr.net/gh/lumeland/markdown-plugins@0.12.0/"
  },
  "tasks": {
    "deploy": {
      "description": "Deploy site to gh-pages",
      "command": "rm -rf _site && deno task build && git worktree add -B gh-pages _site origin/gh-pages && cd _site && git add . && git commit -m 'Deploy' && git push origin gh-pages --force && cd .. && rm -rf _site"
    },
    "index": {
      "description": "Index catalog files and build site",
      "command": "deno run --allow-env --allow-read --allow-write --allow-run --allow-net build.ts"
    },
    "lume": {
      "description": "Run Lume command",
      "command": "deno run --allow-env --allow-read --allow-write --allow-run --allow-net --allow-sys lume/cli.ts"
    },
    "build": {
      "description": "Build the site for production",
      "command": "deno run --allow-env --allow-read --allow-write --allow-run --allow-net --allow-sys build.ts"
    },
    "serve": {
      "description": "Run and serve the site for development",
      "command": "deno task lume -s"
    }
  },
  "compilerOptions": {
    "types": [
      "lume/types.ts"
    ],
    "jsx": "react-jsx",
    "jsxImportSource": "lume"
  },
  "unstable": [
    "fmt-component"
  ],
  "allowScripts": {
    "deny": [
      "npm:sharp"
    ]
  },
  "lint": {
    "plugins": [
      "https://cdn.jsdelivr.net/gh/lumeland/lume@3.2.2/lint.ts"
    ],
    "rules": {
      "exclude": [
        "no-import-prefix"
      ]
    }
  },
  "lock": false
}
