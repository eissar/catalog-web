_site
_cache
.env
/dist

# Ignore only the root catalog clone (external repo), not content/catalog/
/catalog

# Ignore synced catalog content (generated during build)
/content/catalog

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
